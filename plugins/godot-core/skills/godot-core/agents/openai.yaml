interface:
  display_name: "Godot Core"
  short_description: "Plan-first Godot engineering with explicit validation"
  default_prompt: "Use $godot-core to implement or refactor Godot systems with official-doc routing, a short plan, and explicit validation commands."
dependencies:
  tools:
    - type: "mcp"
      value: "openaiDeveloperDocs"
      description: "OpenAI developer docs MCP (read-only)"
      transport: "streamable_http"
      url: "https://developers.openai.com/mcp"
